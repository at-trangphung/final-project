<div class="block2-txt-child2 flex-r p-t-3" id="product_<%= params[:product_id] %>">
  <%= form_with url: favorite_path(@favorite), method: :delete do |form| %>
  <%= form.hidden_field :user_id, value: @service_user.current_user.id %>
   <%= form.hidden_field :product_id, value: params[:product_id] %>
  <button type="submit">
    <i class="fas fa-heart"></i>
  </button>
  <% end %>
</div>